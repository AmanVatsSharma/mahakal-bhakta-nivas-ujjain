import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://www.shrimahakalbhaktniwasujjain.com',
  trailingSlash: 'always',
  build: {
    inlineStylesheets: 'auto',
  },
});
